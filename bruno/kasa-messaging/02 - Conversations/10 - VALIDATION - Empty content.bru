meta {
  name: 10 - VALIDATION - Empty content
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenA}}
}

body:json {
  {
    "content": ""
  }
}

tests {
  test("400 on empty content", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

meta {
  name: 11 - VALIDATION - Whitespace-only content
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenA}}
}

body:json {
  {
    "content": "   "
  }
}

tests {
  test("400: whitespace-only is trimmed and rejected as empty", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

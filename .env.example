# Origine du backend REST (requis). Lue uniquement côté serveur.
BACKEND_API_URL=http://localhost:3000

# URL publique du site, utilisée pour le sitemap et le JSON-LD (optionnel).
# Sur Vercel, VERCEL_PROJECT_PRODUCTION_URL est injecté automatiquement et
# sert de fallback ; en local, http://localhost:3000 est utilisé si absent.
# SITE_URL=http://localhost:3000
